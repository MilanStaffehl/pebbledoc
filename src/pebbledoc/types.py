"""
Type definitions shared by other modules.
"""

from typing import Literal

type AdmonitionStyle = Literal["classic", "mix", "github", "map"]
type DocstringPosition = Literal["default", "pre", "post", "omit"]
